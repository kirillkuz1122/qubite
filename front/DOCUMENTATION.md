# Документация проекта Qubit

Этот документ содержит описание структуры проекта, основных компонентов и логики работы для быстрого погружения и внесения правок.

## 📁 Структура проекта

- `index.html` — Основной файл приложения. Содержит скелет лендинга и личного кабинета (Workspace).
- `404.html` — Страница ошибки 404.
- `css/` — Папка со стилями.
    - `styles.css` — Главный файл стилей (переменные темы, глобальные стили, компоненты).
- `js/` — Папка со скриптами.
    - `app.js` — Вся логика приложения: переключение видов, рендеринг контента, модальные окна, поиск и уведомления.
- `img/`, `desing/`, `source/` — Директории с ассетами (изображения, иконки, дизайн-ресурсы).

## 🚀 Основные концепции

### 1. Переключение представлений (View Management)

Приложение работает как SPA (Single Page Application). Основные контейнеры:

- `#landing-view`: Главная страница (лендинг).
- `#workspace-view`: Личный кабинет игрока (скрыт по умолчанию).

**Ключевые функции в `app.js`:**

- `switchToWorkspace()`: Скрывает лендинг, показывает Workspace и открывает дашборд.
- `switchToLanding()`: Возвращает пользователя на лендинг.
- `ViewManager.open(viewName)`: Динамически меняет содержимое внутри Workspace (Главная, Турниры и т.д.).

### 2. Динамический рендеринг

Контент внутри `workspace-content` генерируется функциями-рендерерами:

- `renderDashboard()`: Генерирует HTML для главной страницы кабинета.
- `renderTournaments()`: Генерирует HTML для списка турниров.
- `renderTeam()`: Генерирует интерфейс для раздела «Команда» (приглашения, создание, вступление).
- `renderTournamentList(data)`: Вспомогательная функция для рендеринга карточек турниров на основе массива данных.

### 3. Система тем (Dark/Light Mode)

Используются CSS-переменные, объявленные в `:root` и `.light-theme`.

- `toggleTheme()`: Переключает тему и сохраняет выбор в `localStorage`.

### 4. Утилиты (Global Utilities)

В начале `app.js` определены глобальные объекты для удобной работы:

- `Loader`: Показ/скрытие загрузчика (`Loader.show()`, `Loader.hide()`).
- `Toast`: Всплывающие уведомления (`Toast.show(title, desc, type)`).
- `Haptic`: Вибрация для мобильных устройств.
- `Sound`: Проигрывание звуковых эффектов (клик, переход).
- `MegaSearch`: Умный поиск с поддержкой исправления раскладки и опечаток.

## 🛠 Инструкция по созданию/исправлению

### Как добавить новый раздел в Workspace?

1. В `index.html` найдите `<nav class="sidebar__nav">` и добавьте новую ссылку с `data-view="my-section"`.
2. В `app.js` внутри `ViewManager.open(viewName)` добавьте условие:
    ```javascript
    else if (viewName === "my-section") {
        this.content.innerHTML = renderMySection();
    }
    ```
3. Создайте функцию `renderMySection()`, которая возвращает строку с HTML.

### Где искать баги?

- **Проблемы с версткой**: Проверьте `css/styles.css`. В начале файла определены переменные цвета (`--bg`, `--fg`, `--accent`).
- **Логика переключений**: Ищите в объекте `ViewManager` в `app.js`.
- **Авторизация и модалки**: Шаблоны модалок хранятся в переменной `DYNAMIC_MODALS_HTML` в `app.js`. Логика открытия — `openModal(id)`.

## 📌 Данные

Данные о турнирах на данный момент захардкожены в массиве `TOURNAMENTS_DATA` внутри `app.js`. При подключении Бэкенда именно здесь нужно будет делать Fetch запрос.
